# Migrations

Apply in filename order against a dedicated KenSapo Supabase project.

Do not run these against the kensapo-alpha database.

```
supabase db push
```

or paste each file into the SQL editor, starting with `20260817100000_extensions.sql`.
